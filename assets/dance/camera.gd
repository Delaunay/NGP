extends Node3D


#@export var rotation_speed: float = 1.0  # Speed of rotation
#@onready var target = $Person  # The object to focus on
#
#func _process(delta):
	#rotate_y(rotation_speed * delta)  # Rotate around Y-axis
	## self.look_at(target.global_transform.origin)  # Keep looking at the target
